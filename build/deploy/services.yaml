apiVersion: v1
kind: Service
metadata:
  name: cockroachdb
  labels:
    app: igru
spec:
  type: LoadBalancer
  ports:
    - name: dashboard
      port: 8080
    - name: db
      port: 26257
  selector:
    app: igru
    database: dev